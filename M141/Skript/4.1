1. CREATE TABLE ... ( ... );
2. INT;
3. DESC tabellenname;
4. SELECT * FROM ... ;
5. INSERT INTO ... VALUES ( ... ) ;
6. wird verwendet, um Daten in einer DB-Tabelle zu ändern;
7. DESC;
8. Gleiche Werte werden nur einmal ausgegeben;
9. AVG(lohn);
10. WHERE ort LIKE 's%gall%';
11. BETWEEN;
12. SELECT ort, COUNT(*) from orte, adressen WHERE ort_fk = ort_id GROUP BY ort;
13. SHOW_DATABASES;
